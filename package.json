{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite build && vite preview",
    "deploy": "gh-pages -d docs"
  },
  "dependencies": {
    "fs": "^0.0.1-security",
    "gh-pages": "^6.3.0",
    "three": "^0.179.1",
    "toml": "^3.0.0"
  },
  "devDependencies": {
    "vite": "^7.1.0",
    "vite-plugin-toml": "^0.8.4"
  }
}
