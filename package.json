{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite build && vite preview",
    "deploy": "gh-pages -d docs"
  },
  "dependencies": {
    "gh-pages": "^6.3.0",
    "three": "^0.179.1"
  },
  "devDependencies": {
    "vite": "^7.1.0"
  }
}
